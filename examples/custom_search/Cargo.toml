[package]
name = "example-custom-search"
description = "An editor searched through the application's own search bar instead of the built-in panel."
version = "0.6.1"
publish = false
edition.workspace = true

[dependencies]
gpui-kit = { workspace = true, features = ["tree-sitter-languages"] }
anyhow.workspace = true

[lints]
workspace = true
